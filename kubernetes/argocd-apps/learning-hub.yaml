apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: learning-hub
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/raghavendrak89/home_lab.git
    targetRevision: main
    path: kubernetes/apps/learning-hub
  destination:
    server: https://kubernetes.default.svc
    namespace: my-learning
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
